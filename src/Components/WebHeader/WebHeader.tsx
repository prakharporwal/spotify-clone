interface IProps {}

const WebHeader = (props: IProps) => {
  return (
    <header className="w-full h-12 bg-black text-white text-right fixed top-0">
      HEADER
    </header>
  );
};

export default WebHeader;
